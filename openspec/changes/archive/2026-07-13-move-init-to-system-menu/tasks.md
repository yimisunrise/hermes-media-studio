## 1. 菜单结构调整

- [x] 1.1 在 `src/app.js` 中将 `init` 菜单项从 `operations` 分组的 `items` 数组移至 `system` 分组的 `items` 数组
- [x] 1.2 验证 JS 语法：运行 `find src -name "*.js" -exec node --check {} \;`（所有报错均为预存的 ES Module 兼容问题，与本次改动无关）
