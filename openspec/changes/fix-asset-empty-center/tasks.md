## 1. CSS 修复

- [x] 1.1 在 `.ms-asset-empty` 样式规则中增加 `grid-column: 1 / -1` 和 `text-align: center`

## 2. 验证

- [x] 2.1 JS 语法检查通过：`node --input-type=module --check < src/business/views/AssetGallery.js`
- [ ] 2.2 空状态在前端呈现时文字水平居中（需在浏览器中确认）
